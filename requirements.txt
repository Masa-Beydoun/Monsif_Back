# -- Web ---------------------------------------------------------------------
Flask==3.1.3
flask-cors==6.0.5
python-dotenv==1.0.1

# -- Summarization (Part 0 - existing) ---------------------------------------
scikit-learn==1.9.0
networkx==3.6.1
nltk==3.10.3
PyArabic==0.6.15
numpy==2.1.3
scipy==1.15.2

# -- Shared RAG stack: BGE-M3 + reranker + hybrid search ---------------------
torch==2.5.1
transformers==4.51.3
sentence-transformers==3.4.1
faiss-cpu==1.9.0
rank-bm25==0.2.2

# -- Part A: laws RAG (dense + sparse via Qdrant) ----------------------------
FlagEmbedding==1.3.4
qdrant-client==1.19.0
peft==0.14.0

# -- Part C: preliminary ruling ----------------------------------------------
# النموذج اللغوي يُستدعى عبر HTTP متوافق مع OpenAI (services/llm_client.py):
#   LLM_BACKEND=hf   → router.huggingface.co  (Llama 3.1 بلا تنزيل أوزان)
#   LLM_BACKEND=groq → api.groq.com
# فلا يلزم أي SDK خاص بالمزوّد، فقط requests.
requests==2.32.3
joblib==1.5.3

# مطلوب فقط لطبقة اقتراح نماذج العقود التي لم تُهاجَر بعد إلى llm_client.
groq==0.13.0

# -- Optional: long-query decomposition via Gemini (LAWS_DECOMPOSE=true) -----
# google-genai==0.8.0
